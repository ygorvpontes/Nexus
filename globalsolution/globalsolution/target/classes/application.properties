spring.application.name=globalsolution
# --- Configura��o do H2 Database (Banco em Mem�ria) ---
# Habilita o console do H2 no navegador
spring.h2.console.enabled=true
# Define a URL do console (mesma URL da conex�o)
spring.h2.console.path=/h2-console

# --- Configura��o do DataSource (Conex�o com o Banco) ---
# A URL da conex�o com o banco H2. O ";DB_CLOSE_DELAY=-1" impede que o banco seja apagado enquanto a app estiver rodando.
spring.datasource.url=jdbc:h2:mem:globalsolutiondb
# O driver do H2 (j� veio na depend�ncia)
spring.datasource.driverClassName=org.h2.Driver
# Usu�rio padr�o do banco H2
spring.datasource.username=sa
# Senha padr�o (em branco)
spring.datasource.password=

# --- Configura��o do JPA (Como o Spring fala com o Banco) ---
# Diz ao Hibernate (JPA) qual "dialeto" SQL usar
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# [MUITO IMPORTANTE] Isso diz ao JPA para criar/apagar as tabelas automaticamente
spring.jpa.hibernate.ddl-auto=create-drop 

# [OPCIONAL, MAS RECOMENDADO] Mostra no console o SQL que o JPA est� executando
spring.jpa.show-sql=true

# --- Configura��o dos Seeds (Dados Iniciais) ---
# Diz ao Spring para carregar e executar o data.sql automaticamente
spring.jpa.defer-datasource-initialization=true